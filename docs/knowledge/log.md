# Knowledge Bundle — Change Log

Chronological history of the OKF-style reference bundle. Newest entries at the bottom.

- 2026-07-02 — module-registry.md — extracted from PROJECT_LOGIC.md [PL-2.2] (imperative [PL-2.2.50] retained in core).
- 2026-07-02 — db-schema.md — extracted from PROJECT_LOGIC.md [PL-3.1] (constraint fact [PL-3.1.1] retained in core).
- 2026-07-02 — index.md, log.md — bundle skeleton created.
- 2026-07-02 — CONTEXT_PROMPT.md [CP-3] — repaired merge corruption at CP-2/CP-3 boundary; restored standalone `## [CP-3]` heading.
- 2026-07-02 — CONTEXT_PROMPT.md [CP-3.6]/[CP-3.7] — deduplicated against PROJECT_LOGIC [PL-4.5]/[PL-6.12] and [PL-6.1]/[PL-6.2]/[PL-6.18]; full rules stay in PROJECT_LOGIC, CP cites indices.
- 2026-07-02 — CONTEXT_PROMPT.md [CP-2] — compressed feature list to one line per feature (rule CP-2.2); detail retained in PROJECT_LOGIC/bundle.
- 2026-07-02 — features/design-system.md — extracted from CONTEXT_PROMPT.md [CP-4] (visual design tokens, reference data).
- 2026-07-02 — rule-map.md — created (feature 002): resolves 295 legacy PL/CP anchors to R-IDs (138) or bundle files (157).
- 2026-07-02 — subagents.md — created (feature 002): full subagent configs moved verbatim from the old AGENTS.md.
- 2026-07-02 — architecture.md, middleware.md, fsm-protocol.md, db-patterns.md, constants.md, testing.md, features-overview.md — created (feature 002, Chunk 2): dissolution of PROJECT_LOGIC.md/CONTEXT_PROMPT.md descriptive content per research.md D6 map.
- 2026-07-03 — RULES.md — added R-ARCH-9, R-UI-12, R-UI-13 (feature 003, audit F-1: rules PL-4.1/CP-3.11/CP-3.47 were lost during 002 consolidation, restored verbatim from git history at `8280d6f^`); amended R-PROC-2 with the CP-3.28.2 incremental-updates principle.
- 2026-07-03 — rule-map.md — repaired 30 rows (feature 003, audit F-1/F-2): 24 D2-table anchors + 6 additional anchors that fell back to `docs/knowledge/index.md`; zero index.md fallback rows remain; CP-5.1 marked retired (scope note obsolete after the 002 file split).
- 2026-07-04 — graph.md — created (feature 004): knowledge-graph usage guide — query/path/explain + rebuild commands, the two freshness channels (code=post-commit hook, docs/semantic=docs-update skill), CLI-absent fallback; supports R-PROC-12.
- 2026-07-14 — module-registry.md, fsm-protocol.md, testing.md, features-overview.md — updated (feature 011): registered the new leaf module `callbacks.py` (single source of truth for callback format, incl. the deliberate `HelpCB` `sep="|"` quirk and why it must not be unified); rewrote the `generic_navigator` entry for the `str | CallbackData` signature, the exact-match registry resolution order and the removal of `PAGINATED_CMDS`; documented the characterized FSM-reset asymmetry (top-level lists reset, detail cards do not) so it is not "evened out" later; added the four-file callback-routing test layer with the format-agnostic harness rationale.
- 2026-07-14 — RULES.md — added R-UI-14 (feature 011): single source of truth for callback format — one CallbackData declaration per parameterized route, `.pack()` on the producer side, `Factory.filter()` on the consumer side, pagination as a declared field; hand-assembling parameterized callback data is prohibited. Amended R-UI-3 (navigator accepts `str | CallbackData`; Defensive Routing now spells out exact-match lookup and by-name parameter access) and R-UI-11 (defensive parsing is the filter's duty; truncating callback data to fit 64 bytes is prohibited — overflow must fail loudly at build time). **ID note**: R-UI-13 was already taken (feature 003, Admin-creation UX branching) — the new rule took the next free ID rather than reusing it.
- 2026-07-17 — db-schema.md, db-patterns.md, fsm-protocol.md, architecture.md — updated (feature 012 / №16, persistent FSM storage): added the `fsm_storage` DDL; documented the `SQLiteStorage` backend and its three unusual traits (the `thread_id` None→0 sentinel that dodges SQLite's distinct-NULLs-in-composite-PK trap on the private-chat path; the deliberate absence of foreign keys; the R-FSM-1 deletion boundary that drops a row only when state is null AND data is empty, protecting the Sterile Interface tracking keys); recorded that state now survives restarts with no TTL and JSON value storage; added the `loader.py → database/db.py` import-graph edge with the note that import-linter does not cover root modules, so the graph is its authoritative record.
- 2026-07-16 — graph.md — corrected the "Model configuration" section: it claimed `DEEPSEEK_API_KEY` is kept in the `.claude/settings.json` `env` block, which was never true (that block carries only `GRAPHIFY_CLAUDE_CLI_MODEL`). The key actually lives in the Windows User environment (`HKCU\Environment`) and is inherited by graphify's process; the project `.env` holds a separate DeepSeek key read by the bot via dotenv, which graphify does not parse. Added a three-row table of the plausible-vs-actual locations, the User-env propagation caveat (only processes started after it was set inherit it), and a note to diagnose via `$env:DEEPSEEK_API_KEY` rather than by grepping config. The stale claim had cost a prior session the feature-011 semantic graph refresh: it grepped settings.json, found nothing, and wrongly concluded the key was absent. Same wording fixed in the docs-update SKILL.md graph-refresh step.
- 2026-07-19 — module-registry.md — updated (feature 014, backend unification): registered `EventService.apply_participation_change` — the single participation-change orchestrator that owns the whole consequence tail (intent-directed mutation via `ManagementService`, join-only organizer notify, all-copies `refresh_announcements`), with structural before/after change detection replacing the "записаны" substring test and lazy service imports keeping the event/announcement/management triangle acyclic (`R-ARCH-4`); also listed the previously-unregistered `check_direct_join_allowed`/`notify_organizers_of_direct_join`. Annotated the two web toggle endpoints (`toggle_participation`, `toggle_event_participation_direct`) as thin callers taking an explicit `action` (join/leave) and delegating to the orchestrator — the announcement endpoint's hand-rolled single-message edit was removed (all copies now refresh), and missing/invalid `action` returns 400. Note: `ManagementService.toggle_event_participation` is left in place but is now unreferenced by production code (the migration removed its last caller) — a dead-code candidate flagged for a separate cleanup, not removed under this feature's scope.
- 2026-07-19 — module-registry.md — dead-code cleanup: removed the orphaned `ManagementService.toggle_event_participation` (the blind state-toggle that feature 014 superseded with explicit-intent `apply_participation_change` + `add_event_participation_action`/`leave_event_action`). It had zero production and test callers since feature 007 moved `leave_event` off it and feature 014 unified the rest; deletion is behavior-neutral (full suite 429 green). Dropped its name from the `services/management_service.py` [PL-2.2.19] method list.
- 2026-07-20 — module-registry.md — updated (feature 015, TMA event authoring + frontend modularization): registered the new event-authoring API router `web/routers/events.py` (`create_event` POST session-only = bot parity; `update_event` PUT per-event `can_edit_event` = authority-parity, no blanket admin gate) and the display-serialization helper `web/serialization.py::display` (un-escapes stored HTML at the JSON boundary, D3). Annotated `web/routers/dashboard.py` (`get_all_events` now returns `end_date`; `get_event_view` returns the server-computed `can_edit` affordance flag; display fields un-escaped) and `web/main.py` (added the `events` router). Rewrote the `web/frontend/**` entry: the `app.js` monolith was deleted and replaced by native ES modules under `web/frontend/js/` (`main`/`router`/`api`/`render` + file-per-screen `screens/*` + `ui/components.js`), with design-system v2 tokens in `style.css`. Added the three new Level-A test modules (`test_events_create`, `test_events_edit`, `test_frontend_contract`). Full suite 440 green.
