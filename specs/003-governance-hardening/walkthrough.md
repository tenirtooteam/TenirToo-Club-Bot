# Walkthrough — Governance Hardening (Rule Retention Repair + Route/Spec-Kit Unification)

Отчёт о реализации фичи `003-governance-hardening`. Исполнитель: Claude Sonnet end-to-end через `speckit-implement`. Два чанка (A: группа F, HARD STOP, B: группа К), классификация полностью снята заранее в research.md D2/D6.

## Changes made (Внесённые изменения)

**Группа F — восстановление правил и ремонт rule-map:**
- Восстановлены дословно из `git show 8280d6f^:...` три правила и одно дополнение: `R-ARCH-9` (неизменность порядка middleware-пайплайна, было PL-4.1), `R-UI-12` (изолированные cancel-клавиатуры + `terminate_input` перед независимыми переходами, было CP-3.11), `R-UI-13` (UX-ветвление при создании сущности админом, было CP-3.47), дополнение `R-PROC-2` принципом инкрементальных правок плана (было CP-3.28.2).
- `docs/knowledge/rule-map.md`: 30 строк отремонтированы (24 по таблице диспозиций research.md D2 + 6 дополнительных, найденных при ремонте). **0 строк** теперь указывают на `docs/knowledge/index.md` (было 10). Счётчики заголовка обновлены: 295 якорей — 161 к R-ID (было 138), 7 курируемых descriptive/retired, 127 к файлам бандла.
- Новый постоянный тест `test_imperatives_map_to_rules` в `tests/test_governance.py`: каждый императивный legacy-якорь обязан резолвиться в реально существующий R-ID либо иметь явную диспозицию `descriptive`/`retired` в `tests/fixtures/imperative_dispositions.txt` (6 записей). Класс потери правил из аудита F-1/F-4 теперь ловится автоматически при каждом прогоне CI, а не ручным аудитом.

**Группа К — линтер v2 и канонизация spec-kit:**
- `local_scripts/prompt_linter.py`: добавлены `find_plan_file(dir)` и `find_checklist_file(dir)` — plan-стадия предпочитает `plan.md` (spec-kit), checklist-стадия предпочитает `tasks.md`, с полным откатом на легаси-имена (`implementation_plan.md`/`task.md`) для исторических фич. Сигнатура `validate_plan()` расширена необязательным параметром `required_h2s` — старые вызовы с одним аргументом работают без изменений (100% обратная совместимость).
- `AGENTS.md`: Route A теперь называет цепочку spec-kit (`PA-1 → specify → clarify → plan → tasks → approval → implement`) как канонический движок; `RNA-1` помечен как legacy-алиас; § COMMAND REGISTRY получил 6 строк `speckit-*`; § RNA-BLUEPRINT переименован в § PLAN CONTENT с явным отображением секций RNA-Blueprint на заголовки `plan.md`; в Route B зафиксирован порядок (PA-1 до specify, clarify после появления спеки).
- `RULES.md`: `R-PROC-2`/`R-PROC-4` теперь называют `plan.md`/`tasks.md` каноническими артефактами (легаси-имена приняты для исторических фич); `R-PROC-2` требует явной HARD-STOP задачи-гейта в каждом чанке.
- `.specify/templates/tasks-template.md`: добавлена обязательная секция «Approval Gates (R-PROC-2)» + конкретные примеры HARD-STOP-задач вставлены на естественных границах чанков (после Foundational, после каждой User Story) — гейт одобрения теперь встроен в артефакт, а не держится на памяти автора плана.
- `docs-update/SKILL.md`: одна строка в Validation-секции называет оба набора целей линтера.

## What was tested (Что тестировалось)

- **TDD-порядок группы F**: `test_imperatives_map_to_rules` написан и зафиксирован красным ДО правок, назвал 18 нерезолвленных якорей (24 из таблицы D2 минус 6, уже задиспозиционированных в T001).
- **TDD-порядок группы К**: 9 новых unit-тестов линтера + 2 journey-теста написаны и провалились коллекцией (`ImportError`) ДО реализации `find_plan_file`/`find_checklist_file`.
- **Обратная совместимость линтера — доказана живьём**: `--dir specs/002-governance-consolidation --stage plan` проходит через легаси-путь (`implementation_plan.md`), `--dir specs/003-governance-hardening --stage plan` — через v2-путь (`plan.md`). Все 9 существующих unit-тестов и 3 существующих journey-теста не тронуты и зелёные.
- **Мутационные проверки (все 3 из контракта)**: (1) повторная привязка императивного якоря к файлу бандла → тест падает; (2) удаление строки диспозиции → тест падает; (3) удаление H2 «Constitution Check» из v2-плана → plan-стадия линтера падает. Все три — восстановление → зелено.
- **Полная регрессия**: 111 passed в конце Chunk A → **122 passed, 1 skipped, 0 failed** в конце Chunk B (+11 от новых тестов линтера).
- **Ручная проверка (quickstart §4)**: AGENTS.md называет команды spec-kit (6 совпадений); шаблон задач содержит 5 упоминаний HARD STOP и 6 цитат R-PROC-2; RULES.md содержит все три новых ID и слово «incremental»; в rule-map — 0 строк-заглушек на index.md.

## Validation results (Результаты валидации)

| Критерий | Результат |
|---|---|
| SC-001 (retention gate) | Зелёный; красный до правок с точным списком из 18 якорей; мутации 1–2 ловятся |
| SC-002 (честность rule-map) | 0 строк на index.md; каждый императивный якорь резолвится или задиспозиционирован |
| SC-003 (правила восстановлены) | R-ARCH-9, R-UI-12, R-UI-13 присутствуют с Legacy-полями; R-PROC-2 дополнен |
| SC-004 (линтер v2 + обратная совместимость) | 24/24 тестов линтера зелёные; фичи 001/002 продолжают линтоваться как раньше |
| SC-005 (канонизация процесса) | AGENTS.md регистрирует spec-kit; шаблон задач содержит паттерн гейтов |
| SC-006 (полная регрессия) | 122 passed, 0 failed |

Обе группы аудита (F и К) закрыты полностью. Начиная с фичи 004 обязателен только комплект артефактов spec-kit (`plan.md`/`tasks.md`/`walkthrough.md`); легаси-имена (`implementation_plan.md`/`task.md`) остаются поддерживаемыми только для исторических фич 001–003.
